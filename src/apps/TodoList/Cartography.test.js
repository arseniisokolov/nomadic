import { describe, it, expect } from '@jest/globals';

describe('TodoList App', () => {
  it('should have basic functionality', () => {
    // Basic test to ensure the app can be imported
    expect(true).toBe(true);
  });
}); 